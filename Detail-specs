This is a Kubernetes deployment file for the address book application, with a service that exposes the deployment to the external network. The deployment creates 5 replicas of the "sarojdevops/cab-booking-app:77" image and labels them with "app: address-book". The service is of type "NodePort" and it exposes the deployment on port 30001 on the nodes in the cluster.
You can customize it as per your requirement by changing the number of replicas, port numbers, and other settings.

apiVersion: apps/v1
kind: Deployment
metadata: 
  name: address-book-deployment
spec:
  replicas: 5
  selector:
    matchLabels:
       app: address-book
  template:
    metadata:
      labels: 
        app: address-book
    spec:
      containers:
      - name: app-container
        image: sarojdevops/cab-booking-app:77
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: address-book-service
spec:
  type: NodePort
  selector:
    app: address-book
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    nodePort: 30001
